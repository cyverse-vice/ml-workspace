# version: 2.1

jobs:
  build:
    docker: 
      - image: cyversevice/ml-workspace:light
    steps:
      - run:
          name: Install Docker client
          command: apt-get add docker-cli
      - checkout
      - setup_remote_docker
      - run: docker build -t cyversevice/ml-workspace:light .



# orbs:
#   docker: circleci/docker@1.3.0
# version: 2.1
# workflows:
#   build-docker-image-only:
#     jobs:
#       - docker/publish:
#           image: cyversevice/ml-tooling:light
  
